import DebugInstrument from "./instruments/debug-instrument";
import SoundfontInstrument from "./instruments/soundfont-instrument";

const Instruments = {
  DebugInstrument,
  SoundfontInstrument,
};

export default Instruments;
